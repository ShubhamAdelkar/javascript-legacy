// With JavaScript array variables, we can store several pieces of data in one place.

// array elements strored in [] seperated by comma ,
let myArray = ["Shubham", 1];
console.log(myArray);

// nest one array in another array
let teams = [
  ["Bulls", 23],
  ["White Sox", 45],
];
console.log(teams);

// acces arrays with thier indexes, its starts from 0 to n.
// const is another way of declaring variabel it cannot be reassigned, released in ES6.
const array = [50, 60, 70];
console.log(array[0]);
const data = array[1];
console.log(data);
