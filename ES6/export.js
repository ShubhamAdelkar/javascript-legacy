// Export a single variable (named export)
export const MAX_SCORE = 100;

// Export a function
export function calculateAverage(scores) {
  // ... calculation logic
  return average;
}

// Export a default item (only one allowed per file)
export default class UserProfile {
  /* ... */
}
